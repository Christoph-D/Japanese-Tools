version: 2.1
orbs:
  shellcheck: circleci/shellcheck@3.4.0
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - shellcheck/install
      - run: shellcheck **/*.sh
